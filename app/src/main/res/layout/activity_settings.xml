<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/settings_gemini_key"
            android:textColor="@color/friday_green"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:textSize="14sp"
            android:layout_marginBottom="6dp" />

        <EditText
            android:id="@+id/apiKeyInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/settings_gemini_key_hint"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_dim"
            android:background="@drawable/bg_input_field"
            android:padding="12dp"
            android:fontFamily="monospace"
            android:inputType="textPassword"
            android:layout_marginBottom="20dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Şehir (hava durumu için)"
            android:textColor="@color/friday_green"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:textSize="14sp"
            android:layout_marginBottom="6dp" />

        <EditText
            android:id="@+id/cityInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="örn. Antalya"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_dim"
            android:background="@drawable/bg_input_field"
            android:padding="12dp"
            android:fontFamily="monospace"
            android:inputType="text"
            android:layout_marginBottom="20dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="OpenWeatherMap API Anahtarı (isteğe bağlı)"
            android:textColor="@color/friday_green"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:textSize="14sp"
            android:layout_marginBottom="6dp" />

        <EditText
            android:id="@+id/weatherKeyInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="openweathermap.org/api adresinden alınır"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_dim"
            android:background="@drawable/bg_input_field"
            android:padding="12dp"
            android:fontFamily="monospace"
            android:inputType="textPassword"
            android:layout_marginBottom="30dp" />

        <Button
            android:id="@+id/saveButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings_save"
            android:textColor="@color/friday_green"
            android:backgroundTint="@color/bg_panel"
            android:fontFamily="monospace"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Gemini API anahtarını aistudio.google.com/apikey adresinden ücretsiz alabilirsin. Kaydettikten sonra ana ekrana dönüp konuşmaya başlayabilirsin."
            android:textColor="@color/text_dim"
            android:fontFamily="monospace"
            android:textSize="11sp" />

    </LinearLayout>
</ScrollView>
